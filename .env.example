# Base URL of your deployed backend's REST API (see backend/README section)
VITE_API_URL=http://localhost:4000/api

# Base URL of your deployed backend for the Socket.IO connection (no /api suffix)
VITE_SOCKET_URL=http://localhost:4000
